import { Callout } from 'nextra-theme-docs'

# Filters

At any location inside PingBack it is possible to open up the filter view by pressing the `CMD+F / STRG+F` shortcut or the `filter` icon in the top right.

## What you can filter by

### Text
*Included in:*
- eventName
- title
- description

### Projects
foobar

<Callout type="default" emoji="💡">
    Use the prefix `p:<projectID / projectName>` to quickly add a project filter.
</Callout>

### Channels

<Callout type="default" emoji="💡">
    Use the prefix `#<channel>` to quickly add a channel filter.
</Callout>

### Event Name

<Callout type="default" emoji="💡">
    Use the prefix `e:<projectID / projectName>` to quickly add an event filter.
</Callout>

### Tags

#### Advanced queries

<Callout type="default" emoji="💡">
    Use the prefix `t:<tag>:<value>` to quickly add a tag filter.
</Callout>
